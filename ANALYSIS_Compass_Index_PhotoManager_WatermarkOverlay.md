# 自定义相机工程：数据需求与使用场景分析总表

> 版本：2025-11-12；适用工程：CustomCamera
> 说明：本表面向 Compass.ets、Index.ets、PhotoManager.ets、WatermarkOverlay.ets 四个核心文件，综合数据类型、更新频率、来源、场景、性能与扩展性，便于后续维护与新增模块快速对齐。最后附预留扩展行，保持与现有架构一致的规范。

| 文件名称 | 核心功能 | 数据需求明细 | 使用场景描述 | 性能指标 | 扩展性考虑 | 备注说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `entry/src/main/ets/pages/Compass.ets` | 卫星寻星助手页：罗盘绘制、位置权限与隐私处理、单次定位、传感器订阅、卫星方位/俯仰/极化计算、气压估算海拔 | - 传感器数据：磁场(`µT`)、方向角(`°`)、气压(`hPa`)；来源 `@ohos.sensor`，期望采样≥10Hz，实际由系统策略决定。<br>- 位置信息：经纬度十进制度，精度建议≥5–6位小数；来源 `@ohos.geoLocationManager` 单次请求，10s 超时，`PRIORITY_ACCURACY`。<br>- 海拔：基于标准海平面气压 `1013.25hPa` 的气压—海拔换算（本地函数）。<br>- 卫星参数：`longitude(°)`、`name`、`description`（本地配置或偏好存储）。<br>- 隐私状态：`preferences` 中的协议同意与时间戳。<br>- 展示信息规范：方位角/俯仰/极化以 `°` 显示；经纬度以 `E/W`、`N/S` + 十进制度；海拔以 `m` 显示。 | - 用户切换到指南针页：隐私检查→请求定位权限→单次定位→订阅方向/气压/磁场→绘制罗盘，显示设备方向与目标卫星方位。<br>- 与主页联动：由 `Index.ets` 通过 `Swiper` 进入/离开；`isActive` 控制传感器订阅生命周期。<br>- 异常场景：权限拒绝或传感器不可用→使用默认位置或降级显示，并记录日志。 | - 罗盘绘制：静态内容缓存，仅在方向变化>0.5°或首次绘制时重绘。<br>- 定位：单次请求10s超时，失败走默认值。<br>- 订阅：方向/气压/磁场保持轻量更新；UI更新节流至必要频次。 | - 支持多卫星参数管理与切换；计算函数本地化、可单元测试。<br>- 天气/地图服务可插拔；隐私策略可扩展。<br>- 错误捕获统一使用 `Error`，满足 `arkts-no-any-unknown`。 | - 已移除无效 `LocationService`，改用 `geoLocationManager`。<br>- 海拔计算从 `AltitudeCalculator` 迁移为本地函数。<br>- `updateLocationFormat` 输出 `E/W`、`N/S` 规范文本。 |
| `entry/src/main/ets/pages/Index.ets` | 相机主页：相机预览与手势、功能按钮、网格/水平指示、水印叠加、与指南针页的 `Swiper` 切换 | - 相机数据：预览帧率、缩放范围与当前倍率、对焦/测光点；来源 `CameraManager/PreviewManager`。<br>- 传感器数据：加速度（水平指示用）；来源 `@kit.SensorServiceKit`。<br>- 水印数据：`WatermarkInfo`（时间、天气、位置、海拔、方向、经纬度、制作单位）。<br>- 权限：相机等由 `PermissionManager` 统一申请与校验。<br>- 展示信息规范：预览填充屏幕；水印按 `previewWidth/Height` 计算尺寸并叠加至左上角。 | - 应用前台：申请权限→启动相机→设置预览尺寸与旋转→初始化缩放与帧率→显示水印。<br>- 交互：双指捏合缩放、点击设焦与测光、按钮切换拍照/录像/网格/水平。<br>- 页面切换：进入指南针页时释放相机资源，返回时恢复并同步按钮状态。<br>- 异常：权限拒绝直接退出；折叠状态变化时释放并重启相机。 | - 预览帧率设置为支持范围内的最大值；缩放与动画响应 < 100ms。<br>- 水印叠加为轻量 UI，不阻塞预览。<br>- 切页时及时释放相机，避免资源竞争。 | - 支持新增页面与更多叠加层；按钮状态与预览模型解耦。<br>- 与 `PhotoManager` 的水印缓存联动：前台预热降低首帧耗时。<br>- 统一权限与生命周期管理，可拓展到更多设备能力。 | - `Swiper` 控制 `currentTab`；`onEnterCompassTab` 释放相机以降功耗。<br>- `flashBlack` 动画用于拍照反馈；`LevelIndicator` 依赖加速度数据。 |
| `camera/src/main/ets/cameramanagers/PhotoManager.ets` | 照片输出与水印处理：动态信息获取、Canvas绘制、图像保存、并发保护与缓存预热 | - 图像数据：`image.PixelMap` 原图与合成图；尺寸与像素比来源 `display`。<br>- 水印信息：`WatermarkInfo` 动态项（天气/位置/海拔/方向/经纬度/制作单位）+ 时间戳；动态项缓存TTL 30s。<br>- 底图缓存：按尺寸与组织名称缓存，TTL 120s，过期释放 `PixelMap`。<br>- 位置快照：`geoLocationManager` 单次请求；失败降级为默认文本。<br>- 图像打包：`image.ImagePacker`，`DEFAULT_JPEG_QUALITY=100`，`PackingDynamicRange.AUTO`。<br>- 展示信息规范：水印文本行次与标签同 `WatermarkOverlay`，单位统一（°、m、经纬度十进制度）。 | - 前台或会话开始时调用 `prewarmWatermarkCaches`（信息+底图）；拍照链路：获取水印→Canvas绘制→保存→回调缩略图。<br>- 异常：处理失败降级为原图保存，并记录堆栈；并发保护避免重复处理。 | - 内存峰值预算≈原图大小×2；按需分块/即时释放；`ImagePacker` 复用并按需释放。<br>- 水印绘制耗时受图片尺寸与像素比影响，预热显著降低首帧。 | - 可接入天气/地图真实API；水印元素扩展（图标、色彩主题）。<br>- 可异步并行任务池加速合成；单元测试校验绘制与保存流程。 | - 统一 `Error` 记录详细日志；`setCallback` 返回缩略图与URL。<br>- 位置字段默认 `获取中.../定位中.../计算中...` 兼容 UI 占位。 |
| `entry/src/main/ets/views/WatermarkOverlay.ets` | 水印叠加组件：八行信息展示、尺寸计算、旋转与定位、双击编辑“制作单位”、持久化存储 | - 输入数据：`WatermarkInfo`（时间、天气、位置、海拔、方向、经纬度、制作单位）。<br>- 本地状态：`previewWidth/Height`、`currentOrganization`、`showEditDialog`、`editingOrganization`。<br>- 时间戳：每秒刷新一次；组织名称持久化到 `preferences`。<br>- 展示信息规范：顶部标题区半透明蓝色；信息行字体 `10–12sp`；经纬度/方向/海拔单位规范（°/m/EW/NS）；整体旋转90°后左上角固定。 | - 置于相机预览之上，`HitTestMode.None` 不拦截预览触控；双击组织名打开编辑框并保存到本地。<br>- 页面显示时打印调试信息，便于核查渲染尺寸与性能。<br>- 异常：偏好读取/写入失败时记录错误，但不影响拍照与预览。 | - 尺寸随预览动态计算（约 1/2 宽 × 0.4 高）；每秒更新时间戳，UI更新轻量。<br>- 组件渲染透明度与阴影优化，保证叠加效果不显著影响帧率。 | - 可扩展为图标化天气/方向显示、主题切换与国际化文案。<br>- 允许外部注入更多信息项（设备状态、电池、电台频段等）。 | - 组织名允许空值（清空）；UI 使用 `bindContentCover` 弹层。<br>- 与 `PhotoManager` 的信息源保持字段一致，便于重用。 |

---

## 预留扩展行（新增文件/模块）

| 文件名称 | 核心功能 | 数据需求明细 | 使用场景描述 | 性能指标 | 扩展性考虑 | 备注说明 |
| --- | --- | --- | --- | --- | --- | --- |
| （TBD：填写相对路径） | （TBD：一句话概述） | - （TBD：核心数据类型/字段/来源/更新频率） | - （TBD：主要操作流程与异常） | - （TBD：关键指标与预算） | - （TBD：扩展点与复用策略） | - （TBD：展示规范与对齐说明） |

> 维护建议：
> - 新增模块请复用本表的字段命名与单位规范，避免歧义。
> - 数据来源优先使用系统能力（传感器/定位），其余通过可插拔的服务接口接入。
> - 性能项统一给出采样/合成/绘制的阈值与预算，便于压测。